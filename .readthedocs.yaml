version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3"

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - complete
        - docs

sphinx:
  configuration: docs/conf.py
